"use client";

import { createTheme } from "@mantine/core";

export const theme = createTheme({
  primaryColor: 'green',
  breakpoints: {
    xs: "36em",
    sm: "48em",
    md: "62em",
    lg: "75em",
    xl: "88em",
  }
});
