import { Loader } from "@mantine/core";

const Loading = () => {
    return (
        <Loader size={80} style={{display: "flex", justifyContent: "center", alignItems: "center"}} />
    )
};

export default Loading;